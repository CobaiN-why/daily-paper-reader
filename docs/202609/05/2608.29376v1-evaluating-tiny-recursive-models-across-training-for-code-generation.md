---
title: Evaluating Tiny Recursive Models Across Training for Code Generation
title_zh: 跨训练过程评估用于代码生成的微型递归模型
authors: "Anjani Sirivella, Aanisha Newaz, Glaucia Melo"
date: 2026-08-29
pdf: "https://arxiv.org/pdf/2608.29376v1"
tags: ["query:ai-se"]
score: 7.0
evidence: 针对代码生成任务的微型递归模型评估研究
tldr: "代码生成依赖大规模transformer成本高，递归模型复用单块增深度省参数，但现有评估只看教师强制的拟合损失或单点准确率，未顾及自由生成与训练动态。本文用约28M参数的TRM-AR做自然语言到Python生成，与参数匹配及深度匹配的对照在40轮、3个种子下持续比较。结果：参数相同时TRM-AR恢复两控制间损失差的45%和生成差的57%，但成本高约175倍；深度相同时则不如更大transformer，优势仅在抗过拟合，排名并会反转。故应沿训练轨迹联合评估拟合与生成，而非单检查点。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.29376v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 728, \"height\": 530, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.29376v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1477, \"height\": 607, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.29376v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 407, \"height\": 293, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.29376v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1330, \"height\": 235, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.29376v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1283, \"height\": 235, \"label\": \"Table\"}]"
motivation: 现有对递归代码生成模型的评估只靠teacher-forced拟合或单检查点，未考察自由运行生成与训练中的表现变化。
method: 在约28M参数的TRM-AR上做自然语言到Python代码生成，与参数匹配和深度匹配的对照模型比较，跨40轮、3种子追踪拟合和生成质量。
result: "等参数时TRM-AR优于参数匹配对照，并恢复两控制间损失差的45%与生成差的57%，成本高175倍；等深度下不如更大transformer，优势仅在抗过拟合。"
conclusion: 评估递归代码生成模型需沿训练轨迹联合考察拟合与生成，不能只看单检查点，以避免排名反转造成的误判。
---

## 摘要
代码生成越来越依赖于大型Transformer模型，其能力随规模增大而提升。然而，这种规模成本高昂，从而引发了对小型模型的需求，尤其是在数据有限的情况下。递归模型通过重复使用单一模块来增加深度，而不是堆叠独立的层，从而解决了这一问题。此类模型通常通过教师强制拟合（在真实前缀上计算的下一个词元损失）或任务准确率在单个检查点进行评估，而代码是由自由运行生成产生的，即模型自主扩展其输出。教师强制下的优势能否在自由运行生成中延续，以及能否在整个训练过程中保持，仍然是一个开放问题。为研究这两点，我们比较了一个约2800万参数的自回归微型递归模型（TRM-AR）在自然语言到Python代码生成上与参数匹配和深度匹配的对照模型，在40个epoch和三种随机种子下追踪其拟合与生成表现。递归模型与深度匹配对照之间的拟合排名发生了两次反转。根据验证损失选取各检查点并审视训练轨迹，可得到一致的比较。在参数相等时，TRM-AR在拟合、生成和泛化方面均优于参数匹配的对照模型，同时恢复了两个对照之间约45%的验证损失差距和57%的生成质量差距，而其每步成本约为参数匹配对照的175倍。然而，在有效深度相同的情况下，较大的Transformer在其验证最优点上拟合与生成更优，这表明TRM-AR的优势在于抵御过拟合，而非更强的能力。这些发现提示，递归代码生成模型应沿训练轨迹对拟合与生成进行联合评估，而非在单个检查点上评估。

## Abstract
Code generation increasingly relies on large transformer models, whose capability advances with scale. Yet such a scale is costly, creating demand for small models, especially where data is limited. Recursive models address this by reusing a single block to add depth rather than stacking independent layers. Such models are typically evaluated by teacher-forced fit (next-token loss on ground-truth prefixes) or task accuracy, at a single checkpoint, whereas code is produced by free-running generation, where the model extends its own output. Whether a teacher-forced advantage survives free-running generation, and whether it holds across training, remains open. To study both, we compare a ~28M-parameter autoregressive Tiny Recursive Model (TRM-AR) on natural-language-to-Python code generation against parameter-matched and depth-matched controls, tracking fit and generation across 40 epochs and three seeds. The fit ranking between the recursive model and the depth-matched control reverses twice. Selecting each checkpoint by validation loss and examining the trajectory yields a consistent comparison. At equal parameters, TRM-AR fits, generates, and generalizes better than the parameter-matched control while recovering approximately 45% of the validation-loss gap and 57% of the generation-quality gap between the two controls, at roughly 175 times the per-step cost of the parameter-matched control. However, at equal effective depth, the larger transformer fits and generates better at its validation optimum, suggesting TRM-AR's advantage lies in resistance to overfitting, not greater capability. These findings suggest that recursive code generation models should be evaluated jointly on fit and generation across the training trajectory rather than at a single checkpoint.