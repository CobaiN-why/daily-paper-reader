---
title: "Beyond Vector Similarity: Hierarchical Context-Aware Graph RAG vs Standard RAG in Enterprise Code Migration"
title_zh: 超越向量相似度：企业代码迁移中的层次化上下文感知图RAG与标准RAG
authors: "Nilesh Jaiswal, Aniket Agrawal, Arjit Shukla, Divya Malhotra, Saurabh Garg, Suchit Puri, Suddhasatwa Bhaumik"
date: 2026-09-11
pdf: "https://arxiv.org/pdf/2609.12464v1"
tags: ["query:ai-se"]
score: 8.0
evidence: 基于LLM的自动化代码迁移与分层图增强检索
tldr: "企业迁移遗留单体到微服务时，标准向量 RAG 仅检索孤立代码块，割裂继承链，导致高编译失败率。本文提出 HCRG：抽取 AST 并映射到 Spanner 属性图，序列化进 Gemini Context Cache，实现拓扑感知、父节点优先的代码翻译。Graph RAG 将 API 幻觉率从 56.4% 降至 16.2%，依赖解析质量从 34.8% 升至 65.9%，父子一致性从 26.7% 升至 45.5%。尽管复杂度一致性下降，它仍为架构合理的自动化迁移提供更可行路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.12464v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1289, \"height\": 1361, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.12464v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1388, \"height\": 1361, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.12464v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1682, \"height\": 552, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.12464v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1765, \"height\": 379, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.12464v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1764, \"height\": 376, \"label\": \"Table\"}]"
motivation: 传统向量 RAG 难以捕获代码拓扑关系，检索到的孤立片段会切断继承链，造成高编译失败率，企业级代码迁移亟需结构感知方案。
method: 提出 HCRG：抽取 AST 并将架构边写入 Spanner 属性图，再序列化为 Gemini Context Cache，实现父节点优先的拓扑感知翻译。
result: "Graph RAG 将 API 幻觉率从 56.4% 降到 16.2%，依赖解析质量从 34.8% 升到 65.9%，父子一致性从 26.7% 升到 45.5%；复杂度一致性降至 46.7%。"
conclusion: Graph RAG 以复杂度一致性下降为代价，显著减少幻觉并提升依赖解析，为架构合理的自动化企业代码迁移提供了更可行路径。
---

## 摘要
随着企业将遗留单体系统现代化为微服务，大语言模型（LLMs）被大量用于自动化代码翻译。然而，传统的基于向量的检索增强生成（标准RAG）难以捕捉拓扑关系。它会获取孤立代码块，从而切断继承链，导致较高的编译失败率。本文提出一种层次化上下文驻留图（HCRG）方法来解决这些局限。我们的流水线使用 tree-sitter 进行抽象语法树（AST）提取，将架构边映射到 Google Cloud Spanner 属性图，并将该结构序列化到 Gemini Context Cache 中，以支持拓扑感知、父节点优先的代码翻译。我们将评估从简单的文本重叠转向定制的7项指标软件工程框架。诸如 CodeBLEU 等传统指标（两种方法均得分 91%）实际上将标准RAG的结构性失败掩盖在语法上看似合理但已损坏的代码背后。实证上，Graph RAG 决定性地缓解了依赖丢失：API 幻觉率从 56.4% 降至 16.2%，依赖解析质量从 34.8% 提升至 65.9%，父子一致性从 26.7% 上升至 45.5%。然而，Graph RAG 也引入了特定的权衡。密集的全局上下文导致 LLM 进行防御性过度工程，使圈复杂度一致性从 71.6% 降至 46.7%，并略微降低文档字符串保留率（从 67.0% 到 61.0%）。最终，尽管以代码复杂度换取幻觉减少，Graph RAG 仍为自动化企业代码库现代化提供了一条更可行、架构上更合理的路径。

## Abstract
As enterprises modernize legacy monolithic systems to microservices, Large Language Models (LLMs) are heavily utilized for automated code translation. However, traditional vector-based Retrieval-Augmented Generation (Standard RAG) struggles to capture topological relationships. It fetches isolated chunks that sever inheritance chains, leading to high compilation failure rates. This paper introduces a Hierarchical Context-Resident Graph (HCRG) methodology to resolve these limitations. Our pipeline uses tree-sitter for Abstract Syntax Tree (AST) extraction, maps architectural edges into a Google Cloud Spanner Property Graph, and serializes this structure into a Gemini Context Cache for topological, parent-first code translation. We shift evaluation from naive text-overlap to a custom 7-metric Software Engineering framework. Traditional metrics like CodeBLEU (which scored 91% for both methods) effectively masked Standard RAG's structural failures behind syntactically plausible but broken code. Empirically, Graph RAG decisively mitigates dependency loss: API hallucination rates dropped from 56.4% to 16.2%, Dependency Resolution Quality improved from 34.8% to 65.9%, and Parent-Child Consistency rose from 26.7% to 45.5%. However, Graph RAG introduces specific trade-offs. The dense global context causes defensive over-engineering by the LLM, reducing Cyclomatic Complexity Consistency from 71.6% to 46.7%, and slightly degrades Docstring Preservation (67.0% to 61.0%). Ultimately, while trading code complexity for reduced hallucinations, Graph RAG provides a substantially more viable, architecturally sound path for automated enterprise codebase modernization.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机与背景）

- **研究背景**：企业正将遗留单体系统（如 Java 单体）迁移到现代微服务架构（如 Python 微服务），LLM 被广泛用于自动化代码翻译。
- **核心问题**：传统 **Standard RAG（向量检索增强生成）** 将代码切成孤立文本块，依赖语义/词法相似度检索，难以捕获继承、调用、导入等拓扑关系。这会导致：
  - 继承链断裂；
  - 父类、接口、远端依赖缺失；
  - API 幻觉严重；
  - 生成代码语法看似合理但编译/运行失败率高。
- **整体含义**：论文主张从“向量相似度”转向“确定性结构感知”的 Graph RAG，提出 **HCRG（Hierarchical Context-Resident Graph）** 方法，将 AST、属性图和 LLM 上下文缓存结合，实现拓扑感知、父节点优先的代码迁移。
- **核心对比**：Standard RAG vs. Graph RAG（HCRG），在 Java-to-Python 企业代码迁移场景中比较结构正确性、依赖解析、幻觉率、复杂度一致性等。

### 2. 论文提出的方法论

#### 2.1 核心思想

- 不再把代码库视为扁平文本语料，而是建模为**确定性图结构**：
  - 节点：类、方法、函数、文件、可调用对象等；
  - 边：`INHERITS`、`CALLS`、`IMPORTS` 等架构依赖。
- 通过 AST 抽取和属性图存储，支持多跳拓扑检索；
- 通过拓扑排序实现**父类先翻译**，再用新生成的父类代码作为子类翻译的上下文；
- 将全局“架构骨架”序列化进 LLM 的 **Context Cache**，提供低延迟“上帝视角”。

#### 2.2 关键技术细节

- **AST 抽取**：使用 `tree-sitter` 对 Java、Python、Go 等多语言进行 AST 解析。
- **图存储**：将 AST 节点、嵌入和架构边写入 **Google Cloud Spanner Property Graph**。
  - 节点示例：`CodeFiles`、`CodeStructures`、`Callables`；
  - 边示例：`INHERITS`、`CALLS`、`IMPORTS`；
  - 支持 GQL 查询；
  - 原生存储 `ARRAY<FLOAT64>` 向量嵌入。
- **拓扑迁移队列**：
  - 查询 `INHERITS`、`CALLS` 边，构建 DAG；
  - 解析循环依赖；
  - 强制父类/基类先翻译；
  - 子类翻译时注入已生成的 Python 父类代码，保证 `super().__init__()`、方法覆写等正确。
- **Context Caching（架构骨架）**：
  - 将仓库高层结构序列化为 JSON 骨架，包括类、父类层级、方法签名；
  - 缓存到 Gemini Context Cache，TTL 为 1 小时；
  - 降低重复输入 token 成本与延迟。
- **统一语义回退**：
  - 拓扑边处理显式依赖；
  - Spanner 向量搜索通过余弦距离补充隐式关系，如松耦合工具函数、非结构化文档。
- **自动仓库重建**：
  - 将 Java 嵌套目录和 PascalCase 转为 Python 扁平 snake_case 模块；
  - 自动生成 `__init__.py`；
  - 保留 YAML、XML、Proto 等非代码资产。

#### 2.3 评估方法论

- 论文认为 CodeBLEU 等词法指标无法反映迁移可用性，因此提出 **7 项软件工程指标**：
  - Code Migration Quality / Migration Quality（CMQ/MQ）
  - Parent-Child Consistency（PCC）
  - Type Hint Completion（THC）
  - Linter Status（StaQ）
  - Dependency Resolution Quality（DRQ）
  - Cyclomatic Complexity Consistency（CCC）
  - Docstring Preservation（DP）
- **加权 Migration Quality（MQ）**：
  - 依赖解析：40%
  - 继承准确：30%
  - 类型完整性：20%
  - 语法正确性：10%
- **幻觉全局查找机制**：
  - `ast.Call` 只有在以下范围都不存在时才算幻觉：
    1. 本地翻译文件；
    2. Python builtins；
    3. 常见集合方法白名单；
    4. Spanner 全局符号表。
  - 该机制避免把跨文件合法调用误判为幻觉。
- **其他指标实现**：
  - DRQ：解析 `ast.Import`、`ast.ImportFrom`，检查 stdlib、内部白名单和 `importlib.util.find_spec`；
  - PCC：从 Java `extends`/`implements` 抽取继承意图，再检查 Python `ast.ClassDef.bases`；
  - THC：检查 `FunctionDef`/`AsyncFunctionDef` 参数和返回值类型注解；
  - StaQ：使用 `pyflakes` 做语法和 lint 检查；
  - CCC：使用 `lizard` 计算 Java 与 Python 圈复杂度，允许 2 或 10% 方差；
  - DP：评估 docstring 保留情况。

### 3. 实验设计

- **数据集 / 场景**：
  - 使用 `spring-petclinic-genai` 仓库；
  - 执行 Java-to-Python 自动化迁移；
  - 同时处理 `.java` 和 `.proto` 文件。
- **Benchmark / 对比方法**：
  - 基线：**Standard RAG（Vector Search）**
    - 递归字符切分：chunk size 800，overlap 150；
    - 嵌入模型：Vertex AI `text-embedding-004`；
    - 检索：KNN + 余弦距离，top k = 5。
  - 实验组：**Graph RAG / HCRG**
    - `tree-sitter` AST 解析；
    - Spanner Property Graph；
    - GQL 遍历深度 3 hops；
    - Vertex AI Context Caching，TTL 1 小时；
    - 拓扑排序，父类优先。
  - 两者生成模型均为 **Gemini 2.5 Pro**，温度 `T = 0`。
- **评估指标**：
  - 传统指标：CodeBLEU；
  - 自定义 7 项 SE 指标：MQ、PCC、THC、StaQ、DRQ、CCC、DP；
  - 幻觉率。
- **实验层次**：
  - 聚合指标对比（表 1）；
  - 类级高方差分析（表 2）；
  - 类级低方差分析（表 3）。

### 4. 资源与算力

- 论文**未明确报告** GPU / TPU 型号、数量、训练时长、推理总时长或云成本。
- 仅可确认使用了以下云与模型资源：
  - Google Cloud Spanner；
  - Vertex AI；
  - Gemini 2.5 Pro；
  - Vertex AI Context Caching，TTL 1 小时；
  - `text-embedding-004` 嵌入模型。
- 论文未涉及模型训练，主要是推理与迁移流水线实验。
- 因此，算力可复现性方面信息不足。

### 5. 实验数量与充分性

- **实验数量**：
  - 主要围绕 **1 个仓库**：`spring-petclinic-genai`；
  - 对比 **2 种范式**：Standard RAG vs. Graph RAG；
  - 报告 **7 项指标 + CodeBLEU + 幻觉率**；
  - 聚合结果 1 组；
  - 类级分析展示高方差 top 3 和低方差 top 3 文件。
- **充分性评价**：
  - 实验能清晰展示结构感知带来的差异，尤其是控制器和测试类；
  - 但覆盖范围有限，只有单一仓库、单一生成模型、单一迁移方向；
  - 未提供多数据集、多语言、多模型、多轮重复实验或统计显著性检验；
  - 未进行严格消融，例如去掉 Context Cache、去掉拓扑排序、去掉向量回退分别的影响。
- **公平性评价**：
  - 两种方法使用相同生成模型和温度，目标仓库相同，具备基本可比性；
  - 但检索策略本身不同，Graph RAG 获得更丰富结构上下文，上下文预算可能不同；
  - 自定义指标和白名单可能引入设计偏差；
  - 缺少人工评估或真实编译/运行通过率作为外部验证。

### 6. 论文的主要结论与发现

- **Graph RAG 显著改善结构完整性**：
  - API 幻觉率：56.4% → 16.2%；
  - 依赖/API 解析质量 DRQ：34.8% → 65.9%；
  - 父子一致性 PCC：26.7% → 45.5%；
  - 类型提示完成度 THC：51.4% → 80.9%；
  - 加权迁移质量 MQ：65.5% → 83.2%。
- **传统词法指标失效**：
  - Standard RAG 与 Graph RAG 的 CodeBLEU 几乎相同：90.6% vs. 91.1%；
  - 说明 CodeBLEU 无法惩罚断裂的 API 契约和幻觉导入。
- **Graph RAG 并非全维度优越**：
  - 圈复杂度一致性 CCC：71.6% → 46.7%，下降 24.9%；
  - 文档字符串保留 DP：67.0% → 61.0%，下降 6.0%；
  - 原因：全局结构上下文导致 LLM 防御性过度工程，以及 prompt 注意力稀释。
- **类级差异明显**：
  - 高耦合控制器/测试类中，Graph RAG 大幅领先；
    - 如 `OwnerController.java`：Standard RAG MQ 3%，Graph RAG 77%；
    - `VisitController.java`：Standard RAG MQ 3%，Graph RAG 80%。
  - 简单 POJO/实体类中，两者接近；
    - 如 `PetType.java` 两者多项指标相同。
- **总体结论**：
  - Graph RAG 以复杂度一致性下降为代价，换取幻觉减少和依赖解析提升；
  - 对企业级自动化代码迁移，Graph RAG 是更可行、架构上更合理的路径；
  - 未来方向是动态 Hybrid RAG，在结构化代码和文档型内容之间智能切换。

### 7. 优点

- **问题定位准确**：指出 Standard RAG 在代码迁移中的根本缺陷是拓扑关系丢失，而非单纯语义召回不足。
- **方法工程完整**：AST 抽取、属性图、GQL 遍历、DAG 拓扑排序、Context Cache、自动仓库重建形成闭环。
- **评估创新**：从 CodeBLEU 转向 7 项 SE 指标，更贴近编译、依赖、继承、类型等真实迁移需求。
- **幻觉判定更合理**：引入全局符号表，避免将合法跨文件调用误判为幻觉。
- **类级方差分析有价值**：揭示 Graph RAG 在复杂控制器/测试类中优势
